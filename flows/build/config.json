{
  "name": "build",
  "description": "Build the distribution of a Cells application",
  "steps": {
    "build": {
      "app": {
        "requires": {
          "cells-app": "^0.7.0"
        }
      }
    }
  }
}
